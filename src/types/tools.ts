export type Tools = {
  calendar: boolean;
  diary: boolean;
  tasks: boolean;
  workouts: boolean;
  shopping: boolean;
  weather: boolean;
  baby: boolean;
  stats: boolean;
  projects: boolean;
};
